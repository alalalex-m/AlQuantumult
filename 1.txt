编写程序，将一串从键盘输入的字符(输入字符数低于20）存入到变量 buffel中，同时将实际输入的宇符数存放在 CL 寄存器中，并在显示器中显示该串字符

STACK1 SEGMENT STACK
	DW 256 DUP(?)
STACK1 ENDS

DATA SEGMENT
MES DB "INPUT a KEY :,AH,ODH, '$';
STRING DB 20DUP(?), ODH,'$';
DATA ENDS

CODE SEGMENT
	ASSUME CS:CODE, DS:DATA,SS:STACK1
START:  MOV AX,DATA
	MOV DS,AX
	XOR CX
	MOV DX, OFFSET MES; 显示 MES 定义的字符
	MOV AH,09H;
	INT 21H;

SSS: 
	MOV AH,01H;
	INT 21H;
	СМР AL, IBH ;ESC
	JE EXIT;
	INC СХ
	MOV BX,OFFSET STRING;
	MOV [BX]AL;
	INC BX;
	JMP SSS;

EXIT: 
	MOV BX,OFFSET STRING;显示 STRING 定义的字符
	MOV AH,09H;
	INT 21H
	MOV AH,4CH;有鍵返回 DoS
	INT 21H;

CODE    ENDS
        END START
