# DSP

## 1. 时域离散信号和系统(1-2)

### 常用序列、序列运算、线性时不变、因果&稳定、差分方程、周期

## 2. 系统频域分析(3-10)

### 时域离散信号傅里叶变换FT(3，4)、z变换(5，6)、逆Z变换(7，8)、频响特性(9，10)

## 3. 离散傅里叶变换(DFT)(11-15)

### DFT、循环卷积、线性卷积、频谱分析

## 4. 快速傅里叶变换(FFT)(16-18)

FFT、DIT-FFT、DIF-FFT

## 5. 网络结构(19-22)

## 6. 无限脉冲数字滤波器(IIR)

### 双线性变换法、脉冲响应不变法、模拟数字频率转换、巴特沃斯

## 7. 有限脉冲数字滤波器(FIR)

### FIR滤波器线性相位特性及窗函数设计

### FIR滤波器设计法比较

1. **窗口法(Window Method)：**
   - **优点：** 窗口法是一种简单直观的方法，易于理解和实现。
   - **缺点：** 在频域上，窗口法的主瓣较宽，导致频率选择性较差。它可能会引入泄漏(leakage)现象，导致滤波器的性能不如其他方法。

2. **频率采样法(Frequency Sampling Method)：**
   - **优点：** 频率采样法可以直接指定滤波器的频率响应，对于特定的频率要求很有用。
   - **缺点：** 对于大阶数的滤波器，计算复杂度可能较高。

3. **最小均方误差法(Least Squares Method)：**
   - **优点：** 最小均方误差法可以用于指定精确的频率响应，而且在滤波器设计中通常产生较小的误差。
   - **缺点：** 对于某些应用，特别是实时系统中，计算复杂度可能较高。

4. **凯泽窗口法(Kaiser Window Method)：**
   - **优点：** 凯泽窗口法是一种窗口法的改进，可以通过调整窗口参数平衡主瓣宽度和副瓣衰减。能够提供较好的频率选择性和副瓣抑制。
   - **缺点：** 需要调整窗口参数以满足不同的设计要求。

5. **频率响应优化法(Optimal Frequency-Domain FIR Filter Design)：**
   - **优点：** 通过优化算法，可以得到在某种准则下最优的频率响应。
   - **缺点：** 计算复杂度可能较高，具体取决于优化算法的选择。

## 8. 多采样数字信号处理

1. **抽取(Upsampling)：**
   - **原理：** 抽取是指在信号中插入零值来增加其采样率。通过在相邻的采样点之间插入零值，可以使信号的频率成倍增加。
   - **操作：** 如果原始信号的采样率为f，那么进行抽取后的新采样率为nf，其中n是一个正整数。抽取后，通常需要对信号进行低通滤波，以消除由零值插入引起的高频分量。

2. **内插(Interpolation)：**
   - **原理：** 内插是指在已知采样点之间插值，以获取在这些点之间的新采样值。内插可以通过不同的插值算法来实现，如线性插值、样条插值等。
   - **操作：** 内插可以用于恢复由于抽取引起的信号的失真。通过在抽取后的信号上应用适当的内插算法，可以获得更平滑和接近原始信号的近似。

1. **抽取(Upsampling)：**
   - **抽取因子(Upsampling Factor)：** 抽取因子表示新采样率与原始采样率之间的比率。如果原始采样率为\(f\)，而抽取因子为\(n\)，则新采样率为\(nf\)。抽取因子通常是一个正整数。

2. **内插(Interpolation)：**
   - **插值因子(Interpolation Factor)：** 插值因子表示在两个已知采样点之间插值的次数。常见的插值因子包括线性插值(factor=2)，三次样条插值等。插值因子通常是一个正整数。

- **抽取因子的计算：** 如果你知道新采样率和原始采样率，可以使用以下公式计算抽取因子：
  抽取因子=新采样率 / 原始采样率

- **插值因子的选择：** 插值因子通常是一个整数，代表在两个已知采样点之间插值的次数。选择插值因子的具体值取决于应用的要求和计算的复杂性。

1. **差分(Difference)：**
   - **操作：** 差分是指计算信号序列中相邻样本之间的差值。对于离散信号\(x[n]\)，其差分序列\(y[n]\)可以表示为：
     \[ y[n] = x[n] - x[n-1] \]
   - **应用：** 差分常用于分析信号的变化趋势，例如在时间序列分析中。

2. **累积(Integration)：**
   - **操作：** 累积是指计算信号序列前面所有样本的总和。对于离散信号\(x[n]\)，其累积序列\(y[n]\)可以表示为：
     \[ y[n] = \sum_{k=0}^{n} x[k] \]
   - **应用：** 累积常用于平滑信号、去除高频噪声以及计算信号的整体趋势。





# 时域离散信号与时域离散系统

##### 下列哪一个单位抽样响应所表示的系统不是因果系统？(C)

1. 𝛿(n)
2. ﻿﻿﻿h(n) = u(n)
3. ﻿﻿﻿h(n) =u(n) - u(n+1)
4. ﻿﻿﻿h(n) =u(n) - u(n-1)

输出y(n)与当前的输入x(n)有关，与过去的输入和过去的输出无关。(F)

信号可以以光信号、声音信号等形式存在吗。(T)

线性时不变时域离散系统常用线性常系数微分方程描述。(F)

一个系统是因果系统的充要条件是，单位序列响应h(n)是因果序列。(F)

判断 y(n)=nx(n)代表的系统是否是时不变系统。(F)

单位阶跃序列与矩形序列的关系是Rᴺ(n) =u(n-N)-u(n)(F)

数字信号处理的主要对象是数字信号，且是采用数值运算的方法达到处理目的。(T)



# 系统频域分析

##### 序列的傅里叶变换等于序列在()上的Z变换。(C)

A. ﻿﻿﻿正虚轴

B. 负虚轴

C. 单位圆

D. 实轴

##### 下面关于时域频域周期离散关系，说法错误的是(A)

A. 时域是连续周期的信号，频域是离散周期的

B. 时域是连续周期的信号，频域是离散的非周期的

C. 时域是离散周期的信号，频域也是离散周期的

D. 时域是连续非周期信号，频域也是连续非周期的



序列的傅里叶变换是频率w 的非周期函数。(F)

为形成因果稳定的滤波器，2N 个极点中只取s平面右半平面的N个极点构成 Ha(s)。(F)

H(Z)为系统的系统函数，它能表征系统的复频域特性吗？(T)

离散傅里叶变换中，有限长序列都是作为周期序列的一个周期来表示的，都隐含有周期性的意思。(T)

序列z变换的收敛域内可以含有极点。(F)

时域离散信号傅里叶变换存在的充分条件是序列绝对可和。(T)

极点影响系统幅度特性的谷值。(F)

# 离散傅里叶变换(DFT)

##### 若序列的长度M，要能够由频域抽样信号X(K)恢复原序列，而不发生时域混叠现象，则频域抽样点数N 需满足的条件是(A)

1. ﻿﻿﻿N≥M
2. ﻿﻿﻿N⩽M
3. ﻿﻿﻿N≥2M
4. ﻿﻿﻿N⩽2M

一个长度为 M 的有限长序列，使其频谱函数N点等间隔采样值能准确恢复原信号，则N大于等于M。(F)

频谱的泄漏不会使得最高频率超过奈奎斯特频率。(F)

DFT 变换区间长度 N 越大，频率分辨力越高？(T)

频域采样就是对序列频谱函数的等间隔离散取样？(T)

时域离散信号傅里叶变换存在的充分条件是序列绝对可和。(T)



# 快速傅里叶変換(FFT)

##### N 点基A就是N=A形式，那么就是要做(C)次的运算

1. ﻿﻿﻿N
2. ﻿﻿﻿N/2
3. ﻿﻿﻿b
4. ﻿﻿﻿A

##### 下列关于 FFT 说法错误的是(B)

A. ﻿﻿﻿FFT 算法就是不断地把长序列的 DFT 分解成几个短序列的 DFT 来减少 DFT 的运算次数

B. ﻿﻿﻿DIT FFT 算法输入序列为自然序列，而输出序列为倒序排列

C. ﻿﻿﻿DIF FFT 算法与 DIT FFT 算法的运算量是一样的

D. ﻿﻿﻿DIF FFT 算法与 DIT FFT 算法的蝶形运算略有不同，DIF FFT 蝶形先加减后相乘而DIT FFT 蝶形先相乘后加减

##### 如下图所示的运算流图符号是(A)基 2FFT 算法的蝶形运算流图符号。

A. ﻿﻿﻿按时间抽取

B. ﻿﻿按頻率抽

C.取既是按时间也是按频率

D. 以上答案都不对



##### 已知序列x1(n)的长度为N1，x2(n)的长度为 N2，对 x1(n)和x2(n)做卷积，得到的线性卷积y1(n)的长度为多少，6点的圆周卷积y2(n)长度为多少(C)

A. N1+N2, N1+N2

B. N1, N1+N2

C. N1+N2-1, 6

D. 8, 8



##### 在N=32的按时间抽取FFT 蝶形图中，从x(n)到 X(k)需要(C)级蝶形运算过程？

A. 8


B. 4


C. 5

D. 2 

# 时域离散系统的网络结构

##### 下列结构中不属于 IIR 滤波器基本结构的是(A)

A. 频率抽样型

B. 级联型

C. 直接型

D. 并联型

##### 下面关于 IIR 和 FIR 滤波器特点，说法不正确的是(C)

A. FIR 滤波器总是稳定的

B. FIR 滤波器适用于对线性相位要求严格的场合

C. IIR 型滤波器适用于对线性相位要求严格的场合

D. IIR 型滤波器不容易做到线性相位



##### 下列关于 IIR 和 FIR滤波器的说法中错误的是(B)

A.从性能上说，IIR 滤波器系统函数的极点可位于单位圆内的任何地方，因此零点和极点相结合，可用较低的阶数获取较高的选择性，所用的存储单元少，计算量小，所以经济高效
B. IIR 滤波器可以得到严格的线性相位
C. IIR 滤波器必须采用递归结构，极点位置必须在单位圆，内否则系统将不稳定
D. 对于同样的滤波器设计指标，FR滤波器所要求的阶数一般比IR 滤波器高

##### 下列关于 FIR 滤波器的说法中正确的是(C)

A. FIR 滤波器可以利用模拟滤波器进行间接法设计
B. 对于相同的幅频特性要求，用 FIR 滤波器实现要比用IIR 滤波器实现阶数低
C. FIR 滤波器容易设计成线性相位特性
D. FIR 滤波器的脉冲响应长度是无限的

IIR 滤波器可以获得严格的线性相位特性。(F)

线性相位滤波器的h(n)一定是无限长单位脉冲响应。(F)

通常 IIR 滤波器具有递归型结构。(T)

FIR滤波器的单位脉冲响应是有限长的。(T)

IIR 并联型结构可以单独调整第K 对零点的位置。(F)

IIR 级联型结构可以单独调整零点位置。(T)



# 无限脉冲响应数字滤波器的设计

##### 采用模拟-数字转换法设计数字滤波器时.平面的左半平面必须映射到乙平面的(A)。

A. 单位圆内部
B. 实轴上
C. 单位圆外部
D. 单位圆上

##### 下列关于用脉冲响应不变法设计IIR 滤波器的论述中正确的是(D)。

A. 有混频线性频率关系
В. 无混频非线性频率关系
C. 无混频线性频率关系

D. 有混频非线性频率关系

为形成因果稳定的滤波器，2N 个极点中只取s平面右半平面的N 个极点构成 Ha(s)。(F)

线性时不变时域离散系统常用线性常系数微分方程描述。(F)



# 有限脉冲响应数字滤波器的设计

##### 若FIR 数字滤波器满足：h(n)=h(N-1-n)，N 为奇数，则该类滤波器可以实现(A)滤波器。

A. 低通，带通
B. 低通，高通，带通，带阻
C. 带通
D. 高通，带通

##### 哪种窗函数的阻带衰减最大？

A. 海宁窗
B. 三角窗
C. 矩形窗
D. 海明窗

##### 线性相位滤波器的幅度函数为：，N 为偶数，则函数H (w)关于(A)

A. w= 0,2pi 奇对称，w = 2pi 偶对称
B. w= 0,2pi 偶对称，w = 2pi 奇对称
C. w= 0, pi,2pi 奇对称
D. w= 0, pi, 2pi 偶对称



##### 已知某 FIR 滤波%器单位抽样响应h(n)的长度为(M+1)，则在下列不同特性的单位抽样响应中可以用来设计线性相位滤波器的是(C)。

1. ﻿﻿﻿h(n)=h(M+n)
2. ﻿﻿﻿h(n)=h(M-n+1)
3. ﻿﻿﻿h(n)=-h(M-n)
4. ﻿﻿﻿h(n)=-h(M-n+1)

FIR 网络结构特点是有反馈支路，即有环路。(F)

线性相位滤波器的h(n)一定是无限长单位脉冲响应。(F)

窗函数的主瓣形成了纹波？(F)
